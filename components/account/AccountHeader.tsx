import SiteNav from 'components/nav';

// The account pages' masthead: the shared bar, which already knows the person
// and draws their initials.
export default function AccountHeader() {
  return <SiteNav tone="light" />;
}
