import SiteNav from 'components/nav';

// The masthead of the content pages (the legal pages, the 404), which render
// in the older light theme below it.
export default function MainHeader() {
  return <SiteNav tone="light" />;
}
