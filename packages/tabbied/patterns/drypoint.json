{
  "name": "Drypoint",
  "slug": "drypoint",
  "galleryOrder": 902,
  "description": "Drypoint: lines that thicken steadily across the plate, the way the needle digs in as it goes.",
  "tags": [
    "stripes",
    "lines",
    "blocks",
    "grid"
  ],
  "mood": [
    "retro",
    "playful"
  ],
  "density": "dense",
  "goodFor": [
    "textile",
    "wallpaper",
    "poster",
    "packaging"
  ],
  "palette": [
    "#F0EAD6",
    "#264653",
    "#2A9D8F",
    "#E9C46A",
    "#F4A261",
    "#E76F51"
  ],
  "colors": {
    "min": 2,
    "max": 6,
    "default": 6
  },
  "svgExportNote": "The stripe mask can sit up to a pixel away from the on-screen rendering, and its internal coordinates use calc(), which some design tools don't evaluate.",
  "options": [
    {
      "id": "grid",
      "displayName": "Columns and rows",
      "type": "ButtonSelectGroup",
      "default": "6x9",
      "options": [
        "2x3",
        "4x6",
        "6x9",
        "8x12",
        "10x15"
      ],
      "replace": "${grid}"
    },
    {
      "id": "frequency",
      "displayName": "Frequency",
      "type": "Slider",
      "default": 1,
      "min": 0.2,
      "max": 1,
      "step": 0.1,
      "replace": "${shapeFrequency}"
    }
  ],
  "code": {
    "style": "--rule: ( @random(${shapeFrequency}) { background: @p(var(--color1), var(--color2), var(--color3), var(--color4), var(--color5)); -webkit-mask: @var(--texture); mask: @var(--texture); -webkit-transform: rotate(@pick(0deg, 90deg)); transform: rotate(@pick(0deg, 90deg)); } -webkit-transition: ease 450ms; transition: ease 450ms; );",
    "doodle": ":doodle { @grid: ${grid}; @size: ${width} ${height}; overflow:hidden; text-align:center; box-sizing:border-box; --texture: @svg(viewBox: 0 0 100 100; rect*6 { x: calc(@n(-1) * 16.6 + 2); y: -1; width: calc(2 + @n(-1) * 1.8); height: 102; fill: #000 }) } :container { background: var(--color0); overflow:hidden; }"
  }
}
