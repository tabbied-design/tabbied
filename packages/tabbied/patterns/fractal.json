{
  "name": "Fractal",
  "slug": "fractal",
  "galleryOrder": 908,
  "galleryWhite": true,
  "description": "Self-similar all the way down: the inner grid uses the same open-or-shut rule as the outer one.",
  "tags": [
    "squares",
    "grid",
    "mosaic",
    "blocks"
  ],
  "mood": [
    "retro",
    "bold",
    "playful"
  ],
  "density": "dense",
  "goodFor": [
    "poster",
    "wallpaper",
    "textile",
    "og-image"
  ],
  "palette": [
    "#1A1A2E",
    "#16213E",
    "#0F3460",
    "#E94560",
    "#F5F5F5",
    "#53BF9D"
  ],
  "colors": {
    "min": 2,
    "max": 6,
    "default": 6
  },
  "svgExportNote": "The nested mask is exported as clean vector shapes - the faint seams visible on screen come from browser rasterization and won't appear in the SVG, so it can differ from the screen by a hair.",
  "options": [
    {
      "id": "grid",
      "displayName": "Columns and rows",
      "type": "ButtonSelectGroup",
      "default": "6x9",
      "options": [
        "2x3",
        "4x6",
        "6x9",
        "8x12",
        "10x15"
      ],
      "replace": "${grid}"
    },
    {
      "id": "frequency",
      "displayName": "Frequency",
      "type": "Slider",
      "default": 1,
      "min": 0.2,
      "max": 1,
      "step": 0.1,
      "replace": "${shapeFrequency}"
    }
  ],
  "code": {
    "style": "--rule: ( @random(${shapeFrequency}) { background: @p(var(--color1), var(--color2), var(--color3), var(--color4), var(--color5)); -webkit-mask: @var(--notch); mask: @var(--notch); -webkit-mask-size: 100% 100%; mask-size: 100% 100%; } -webkit-transition: ease 450ms; transition: ease 450ms; );",
    "doodle": ":doodle { @grid: ${grid}; @size: ${width} ${height}; overflow:hidden; text-align:center; box-sizing:border-box; --notch: @doodle(@grid: 3 / 100%; background: #000; @nth(5) { background: #0000; }) } :container { background: var(--color0); overflow:hidden; }"
  },
  "sizing": {
    "cellMultiple": 3
  }
}
