{
  "name": "Torsion",
  "slug": "torsion",
  "galleryOrder": 638,
  "galleryWhite": true,
  "description": "One square per cell, each turned a fixed fraction further than its neighbor, so the grid twists a full quarter-turn corner to corner.",
  "tags": [
    "squares",
    "grid",
    "radial"
  ],
  "mood": [
    "playful",
    "festive",
    "bold"
  ],
  "density": "dense",
  "goodFor": [
    "poster",
    "og-image"
  ],
  "palette": [
    "#141233",
    "#6C4AB6",
    "#3EECFF",
    "#FF3D8B",
    "#F5DD32",
    "#3FFFB2"
  ],
  "colors": {
    "min": 2,
    "max": 6,
    "default": 6
  },
  "options": [
    {
      "id": "grid",
      "displayName": "Columns and rows",
      "type": "ButtonSelectGroup",
      "default": "8x12",
      "options": [
        "2x3",
        "4x6",
        "6x9",
        "8x12",
        "10x15"
      ],
      "replace": "${grid}"
    },
    {
      "id": "frequency",
      "displayName": "Frequency",
      "type": "Slider",
      "default": 1,
      "min": 0.2,
      "max": 1,
      "step": 0.1,
      "replace": "${shapeFrequency}"
    }
  ],
  "code": {
    "style": "--rule: ( @random(${shapeFrequency}) { :after { content: ''; position: absolute; inset: 12%; background: @p(var(--color1), var(--color2), var(--color3), var(--color4), var(--color5)); -webkit-transform: rotate(@calc(90 * (@x + @y) / (@X + @Y))deg); transform: rotate(@calc(90 * (@x + @y) / (@X + @Y))deg); -webkit-transition: ease 450ms; transition: ease 450ms; } } -webkit-transition: ease 450ms; transition: ease 450ms; );",
    "doodle": ":doodle { @grid: ${grid}; @size: ${width} ${height}; overflow:hidden; text-align:center; box-sizing:border-box } :container { background: var(--color0); overflow:hidden; }"
  }
}
